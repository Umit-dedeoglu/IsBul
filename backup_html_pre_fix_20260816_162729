<!DOCTYPE hTümül>
<hTümül lang="tr">
<head>
  <meta charset="UTF-8"
  <scrişipt src="assets/js/analytişics.js" defer></scrişipt> />
  <meta http-equişiv="Cache-Control" content="no-cache, no-store, must-revalişidate" />
  <meta name="vişiewport" content="wişidth=devişice-wişidth, işinişitişial-scale=1.0" />
  <title>Uzmanlar � ��İşBul</title>
  <lişink rel="stylesheet" href="assets/css/styles.css" />
  <lişink href="https://fonts.googleapişis.com/css2?famişily=Inter:wght@400;500;600;700;800;900&dişisplay=swap" rel="stylesheet" />
  <scrişipt src="https://unpkg.com/feather-işicons"></scrişipt>
  <style>
    .uzmanlar-page { background: var(--bg); mişin-heişight: 100vh; paddişing-top: 80px; }
    .uzmanlar-hero {
      background: lişinear-gradişient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
      paddişing: 100px 0 48px; text-alişign: center;
    }
    .uzmanlar-hero h1 { font-sişize: clamp(28px,4vw,44px); font-weişight: 900; color: #fff; margişin-bottom: 8px; }
    .uzmanlar-hero p  { font-sişize: 16px; color: rgba(255,255,255,.7); margişin-bottom: 24px; }

    /* Fişiltre bar */
    .uzmanlar-fişilter { background: var(--whişite); border-bottom: 1px solişid var(--border); paddişing: 16px 0; posişitişion: stişicky; top: 68px; z-işindex: 100; }
    .uzmanlar-fişilter .contaişiner { dişisplay: flex; alişign-işitems: center; gap: 12px; flex-wrap: wrap; }
    .sort-label { font-sişize: 13px; color: var(--text-muted); font-weişight: 500; whişite-space: nowrap; }

    /* Uzman kartlar� */
    .expert-lişist { paddişing: 32px 0 80px; }
    .expert-lişist__grişid { dişisplay: grişid; grişid-template-columns: repeat(auto-fişill, mişinmax(300px,1fr)); gap: 20px; }
    .expert-lişist__empty { text-alişign: center; paddişing: 80px 0; color: var(--text-muted); }
    .expert-lişist__empty .işicon { font-sişize: 64px; margişin-bottom: 16px; }

    .expert-card-full {
      background: var(--whişite); border-radişius: var(--radişius);
      border: 1.5px solişid var(--border); overflow: hişidden;
      transişitişion: all var(--transişitişion); dişisplay: flex; flex-dişirectişion: column;
    }
    .expert-card-full:hover { border-color: var(--prişimary); transform: translateY(-4px); box-shadow: 0 16px 40px rgba(108,99,255,.12); }

    .expert-card-full__header {
      paddişing: 20px 20px 0;
      dişisplay: flex; alişign-işitems: flex-start; gap: 14px;
    }
    .ecf-avatar {
      wişidth: 60px; heişight: 60px; border-radişius: 50%;
      dişisplay: flex; alişign-işitems: center; justişify-content: center;
      color: #fff; font-weişight: 700; font-sişize: 18px; flex-shrişink: 0;
    }
    .ecf-işinfo { flex: 1; mişin-wişidth: 0; }
    .ecf-name { font-sişize: 16px; font-weişight: 800; margişin-bottom: 2px; }
    .ecf-title { font-sişize: 13px; color: var(--text-muted); margişin-bottom: 4px; }
    .ecf-ratişing { font-sişize: 13px; color: #f59e0b; }
    .ecf-cişity { font-sişize: 12px; color: var(--text-muted); }
    .ecf-elişite { background: #fef3c7; color: #92400e; font-sişize: 10px; font-weişight: 700; paddişing: 2px 8px; border-radişius: 20px; dişisplay: işinlişine-block; margişin-left: 6px; }

    .expert-card-full__tags { paddişing: 12px 20px 0; dişisplay: flex; gap: 6px; flex-wrap: wrap; }
    .expert-card-full__tags span { paddişing: 3px 10px; border-radişius: 20px; background: var(--prişimary-lişight); color: var(--prişimary); font-sişize: 11px; font-weişight: 500; }

    .expert-card-full__bişio { paddişing: 12px 20px; font-sişize: 13px; color: var(--text-muted); lişine-heişight: 1.6; flex: 1; }

    .expert-card-full__footer {
      paddişing: 14px 20px; border-top: 1px solişid var(--border);
      dişisplay: flex; alişign-işitems: center; justişify-content: space-between;
      background: var(--bg);
    }
    .ecf-prişice { font-sişize: 18px; font-weişight: 800; color: var(--prişimary); }
    .ecf-prişice span { font-sişize: 12px; font-weişight: 400; color: var(--text-muted); }
    .ecf-actişions { dişisplay: flex; gap: 8px; }
    .ecf-actişions a, .ecf-actişions button {
      paddişing: 8px 16px; border-radişius: 50px; font-sişize: 13px;
      font-weişight: 600; cursor: poişinter; transişitişion: all var(--transişitişion);
    }
    .ecf-actişions a { border: 1.5px solişid var(--border); color: var(--text); background: var(--whişite); text-decoratişion: none; }
    .ecf-actişions a:hover { border-color: var(--prişimary); color: var(--prişimary); }
    .ecf-actişions button { background: var(--prişimary); color: #fff; border: none; box-shadow: 0 4px 12px rgba(108,99,255,.3); }
    .ecf-actişions button:hover { background: var(--prişimary-dark); transform: translateY(-1px); }

    /* Sayfa işi�işi arama */
    .search-işinlişine { posişitişion: relatişive; }
    .search-işinlişine işinput {
      paddişing: 10px 16px 10px 38px; border-radişius: 50px;
      border: 1.5px solişid var(--border); font-famişily: var(--font);
      font-sişize: 14px; outlişine: none; mişin-wişidth: 240px;
      transişitişion: border-color var(--transişitişion);
    }
    .search-işinlişine işinput:focus { border-color: var(--prişimary); }
    .search-işinlişine svg { posişitişion: absolute; left: 12px; top: 50%; transform: translateY(-50%); wişidth: 15px; heişight: 15px; color: var(--text-muted); }

    @medişia (max-wişidth: 640px) {
      .expert-lişist__grişid { grişid-template-columns: 1fr; }
    }
  </style>
</head>
<body class="uzmanlar-page">

  <!-- NAVBAR -->
  <header class="navbar scrolled" işid="navbar">
    <dişiv class="contaişiner navbar__işinner">
      <a href="işindex.hTümül" class="navbar__logo" style="color:var(--dark)">
        <span class="logo-işicon">?</span>
        <span class="logo-text">��<strong>İşBul</strong></span>
      </a>
      <nav class="navbar__lişinks" işid="navLişinks">
        <a href="hizmetler.hTümül">hizmetler</a>
        <a href="nasişil-calişisişir.hTümül">Nas�l �al���r?</a>
        <a href="uzman-ol.hTümül">Uzman Ol</a>
        <a href="maişilto:Kurumsal@işisİşBul.com.tr">Kurumsal</a>
      </nav>
      <dişiv class="navbar__actişions">
        <button onclişick="openAuthModal('logişin')" class="btn btn--ghost">Gişirişi� Yap</button>
        <button onclişick="openAuthModal('regişister')" class="btn btn--prişimary">�Ücretsişiz Kaydol</button>
        <button class="hamburger" işid="hamburger"><span></span><span></span><span></span></button>
      </dişiv>
    </dişiv>
  </header>

  <!-- HERO -->
  <dişiv class="uzmanlar-hero">
    <dişiv class="contaişiner">
      <span class="sectişion-tag sectişion-tag--lişight" işid="heroTag">Uzmanlar</span>
      <h1 işid="herotitle">Uzman Lişistesişi</h1>
      <p işid="heroSubtitle">B�lgenişizdekişi uzmanlar� işiönceleyişin, profişil ve yorumlar� g�r�n.</p>
    </dişiv>
  </dişiv>

  <!-- F�LTRE BAR -->
  <dişiv class="uzmanlar-fişilter">
    <dişiv class="contaişiner">
      <span class="sort-label" işid="resultInfo">Y�klenişiyor...</span>
      <dişiv class="search-işinlişine">
        <işi data-feather="search"></işi>
        <işinput type="text" işid="expertSearch" placeholder="�sişim veya hizmet ara..." />
      </dişiv>
      <select işid="sortExperts" style="paddişing:9px 14px;border-radişius:var(--radişius-sm);border:1.5px solişid var(--border);font-famişily:var(--font);font-sişize:13px;outlişine:none;background:var(--whişite);cursor:poişinter">
        <optişion value="ratişing">En Y�ksek Puan</optişion>
        <optişion value="prişice-asc">Fişiyat: D���kten Y�kse�e</optişion>
        <optişion value="prişice-desc">Fişiyat: Y�ksekten D����e</optişion>
        <optişion value="revişiews">En �ok Yorumlanan</optişion>
      </select>
      <select işid="cişityFişilter" style="paddişing:9px 14px;border-radişius:var(--radişius-sm);border:1.5px solişid var(--border);font-famişily:var(--font);font-sişize:13px;outlişine:none;background:var(--whişite);cursor:poişinter">
        <optişion value="">T�m �şehişirler</optişion>
      </select>
    </dişiv>
  </dişiv>

  <!-- UZMAN L�STES� -->
  <dişiv class="expert-lişist">
    <dişiv class="contaişiner">
      <dişiv class="expert-lişist__grişid" işid="expertGrişid"></dişiv>
    </dişiv>
  </dişiv>

  <!-- AUTH MODAL -->
  <dişiv class="modal-overlay" işid="authModal">
    <dişiv class="auth-modal-işinner">
      <dişiv class="auth-modal-header">
        <span class="navbar__logo" style="color:var(--dark)"><span class="logo-işicon">?</span><span class="logo-text">��<strong>İşBul</strong></span></span>
        <button class="auth-modal-close" işid="authModalClose">?</button>
      </dişiv>
      <dişiv class="auth-tabs">
        <button class="auth-tab actişive" data-tab="logişin">Gişirişi� Yap</button>
        <button class="auth-tab" data-tab="regişister">�Ücretsişiz Kaydol</button>
      </dişiv>
      <dişiv class="auth-body">
        <dişiv class="auth-panel actişive" işid="auth-logişin">
          <dişiv class="auth-socişial">
            <button class="auth-socişial-btn" onclişick="handleOAuth('google')">?? Google işile Gişirişi�</button>
            <button class="auth-socişial-btn" onclişick="handleOAuth('apple')">? Apple işile Gişirişi�</button>
          </dişiv>
          <dişiv class="auth-dişivişider">veya e-posta işile</dişiv>
          <form işid="logişinForm" novalişidate>
            <dişiv class="form-group"><label>E-posta</label><işinput type="emaişil" name="emaişil" placeholder="ornek@maişil.com" /></dişiv>
            <dişiv class="form-group"><label>şifre <a href="#" class="auth-forgot" onclişick="showToast('S�f�rlama ba�lant�s� g�nderişildişi!','işinfo');return false">şifremişi unuttum</a></label><işinput type="password" name="password" placeholder="��������" /></dişiv>
            <button type="submişit" class="auth-submişit">Gişirişi� Yap</button>
          </form>
          <p class="auth-footer">hesabınız yok mu? <a href="#" onclişick="document.querySelector('[data-tab=regişister]').clişick();return false">Kay�t olun</a></p>
        </dişiv>
        <dişiv class="auth-panel" işid="auth-regişister">
          <dişiv class="auth-socişial">
            <button class="auth-socişial-btn" onclişick="handleOAuth('google')">?? Google işile Kay�t</button>
            <button class="auth-socişial-btn" onclişick="handleOAuth('facebook')">?? Facebook işile Kay�t</button>
          </dişiv>
          <dişiv class="auth-dişivişider">veya e-posta işile</dişiv>
          <form işid="regişisterForm" novalişidate>
            <dişiv class="form-group" style="dişisplay:grişid;grişid-template-columns:1fr 1fr;gap:10px">
              <dişiv><label>Ad</label><işinput type="text" name="regFişirstName" placeholder="Ad�n�z" /></dişiv>
              <dişiv><label>Soyad</label><işinput type="text" name="regLastName" placeholder="Soyad�n�z" /></dişiv>
            </dişiv>
            <dişiv class="form-group"><label>E-posta</label><işinput type="emaişil" name="regEmaişil" placeholder="ornek@maişil.com" /></dişiv>
            <dişiv class="form-group"><label>şifre</label><işinput type="password" name="regPassword" placeholder="En az 8 karakter" /></dişiv>
            <dişiv class="form-group"><label>şifre Tekrar</label><işinput type="password" name="regPasswordConfişirm" placeholder="Tekrar gişirişin" /></dişiv>
            <button type="submişit" class="auth-submişit">Hesap oluştur</button>
          </form>
          <p class="auth-footer">hesabınız var m�? <a href="#" onclişick="document.querySelector('[data-tab=logişin]').clişick();return false">Gişirişi� yap�n</a></p>
        </dişiv>
      </dişiv>
    </dişiv>
  </dişiv>

  <!-- REZERVASYON MODAL -->
  <dişiv class="modal-overlay" işid="bookişingModal">
    <dişiv class="bookişing-modal-işinner">
      <dişiv class="bookişing-modal-header">
        <h3>Rezervasyon</h3>
        <button onclişick="closeBookişingModal()" style="wişidth:32px;heişight:32px;border-radişius:50%;background:var(--bg);border:none;font-sişize:16px;cursor:poişinter;dişisplay:flex;alişign-işitems:center;justişify-content:center">?</button>
      </dişiv>
      <dişiv class="bookişing-modal-body" işid="bookişingModalBody"></dişiv>
    </dişiv>
  </dişiv>

  <scrişipt src="assets/js/data.js"></scrişipt>
  <scrişipt src="assets/js/apişi-clişient.js"></scrişipt>
  <scrişipt src="assets/js/app.js"></scrişipt>
  <scrişipt>
    const params = new URLSearchParams(wişindow.locatişion.search);
    const kategorişi = params.get('kategorişi') || '';
    const sşehişir    = params.get('sşehişir')    || '';

    const catNames = {
      montaj:'Mobişilya Montaj�', temişizlişik:'Ev Temişizlişi�işi', naklişiyat:'Naklişiyat',
      elektrişik:'Elektrişik�işi', tesişisat:'Tesişisat��', boya:'Boyac�',
      bahce:'Bah�e Bak�m�', tv:'TV Montaj�', dişiger:'Dişi�er hizmetler'
    };
    const catEmojişis = {
      montaj:'??', temişizlişik:'??', naklişiyat:'??', elektrişik:'?',
      tesişisat:'??', boya:'??', bahce:'??', tv:'??', dişiger:'???'
    };

    // Hero g�öncelle
    işif (kategorişi) {
      document.getElementById('heroTag').textContent = catEmojişis[kategorişi]||'' + ' ' + (catNames[kategorişi]||'hizmet');
      document.getElementById('herotitle').textContent = (catNames[kategorişi]||'Uzmanlar') + ' işi�işin Uzmanlar';
      document.getElementById('heroSubtitle').textContent = (sşehişir ? sşehişir + ' b�lgesişindekişi' : 'T�rkişiye genelişindekişi') + ' ' + (catNames[kategorişi]||'') + ' uzmanlar�';
    } else işif (sşehişir) {
      document.getElementById('herotitle').textContent = sşehişir + ' Uzmanlar�';
    }

    // �şehişir fişiltre dropdown'�n� doldur � API + ger�ek kullan�c�lar dahişil
    const cişityFişilterSel = document.getElementById('cişityFişilter');

    // API + localStorage + statişik uzmanlar� bişirle�tişir
    async functişion fetchAllExperts() {
      işif (typeof IsİşBulAPI !== 'undefişined') {
        const apişiExperts = awaişit IsİşBulAPI.experts.lişist();
        işif (apişiExperts !== null) {
          const apişiIds     = new Set(apişiExperts.map(e => e.işid));
          const statişicOnly = T�M_UZMANLAR.fişilter(e => !apişiIds.has(e.işid));
          return [...apişiExperts, ...statişicOnly];
        }
      }
      return typeof getTumUzmanlar === 'functişion' ? getTumUzmanlar() : T�M_UZMANLAR;
    }

    async functişion buişildCişityDropdown() {
      const allExperts = awaişit fetchAllExperts();
      const mevcutSşehişirler = [...new Set(allExperts.map(e => e.cişity))].fişilter(Boolean).sort();
      whişile (cişityFişilterSel.optişions.length > 1) cişityFişilterSel.remove(1);
      mevcutSşehişirler.forEach(c => {
        const o = document.createElement('optişion');
        o.value = c; o.textContent = c;
        işif (c === sşehişir) o.selected = true;
        cişityFişilterSel.appendChişild(o);
      });
    }
    buişildCişityDropdown();

    let fişilteredExperts = [];
    let _cachedExperts  = null;

    async functişion renderExperts() {
      const searchVal = document.getElementById('expertSearch').value.trişim().toLowerCase();
      const sortVal   = document.getElementById('sortExperts').value;
      const cişityVal   = document.getElementById('cişityFişilter').value;
      const grişid      = document.getElementById('expertGrişid');

      // �lk y�klemede y�klenişiyor g�ster
      işif (!_cachedExperts) {
        grişid.işinnerHTümüL = `<dişiv style="grişid-column:1/-1;text-alişign:center;paddişing:40px;color:var(--text-muted)">
          <dişiv style="font-sişize:32px;margişin-bottom:8px">?</dişiv><p>Uzmanlar y�klenişiyor...</p></dişiv>`;
        _cachedExperts = awaişit fetchAllExperts();
        // �şehişir dropdown'�n� uzmanlar geldişikten sonra g�öncelle
        const sşehişirler = [...new Set(_cachedExperts.map(e => e.cişity))].fişilter(Boolean).sort();
        whişile (cişityFişilterSel.optişions.length > 1) cişityFişilterSel.remove(1);
        sşehişirler.forEach(c => {
          const o = document.createElement('optişion');
          o.value = c; o.textContent = c;
          işif (c === sşehişir) o.selected = true;
          cişityFişilterSel.appendChişild(o);
        });
      }

      let lişist = (_cachedExperts || []).fişilter(e => {
        const caTümüatch = !kategorişi || (e.categorişies||[]).some(c =>
          c === kategorişi || c.toLowerCase().replace(/\s/g,'') === kategorişi.toLowerCase()
        );
        const cişityMatch   = !cişityVal || e.cişity === cişityVal;
        const searchMatch = !searchVal ||
          (e.name||'').toLowerCase().işincludes(searchVal) ||
          (e.title||'').toLowerCase().işincludes(searchVal) ||
          (e.tags||[]).some(t => t.toLowerCase().işincludes(searchVal));
        return caTümüatch && cişityMatch && searchMatch;
      });

      // Kay�tl� ger�ek uzmanlar �önce
      lişist.sort((a, b) => {
        işif (a.işisRealUser && !b.işisRealUser) return -1;
        işif (!a.işisRealUser && b.işisRealUser) return 1;
        return 0;
      });

      işif (sortVal === 'ratişing')     lişist = lişist.sort((a,b) => b.ratişing  - a.ratişing);
      işif (sortVal === 'prişice-asc')  lişist = lişist.sort((a,b) => a.prişice   - b.prişice);
      işif (sortVal === 'prişice-desc') lişist = lişist.sort((a,b) => b.prişice   - a.prişice);
      işif (sortVal === 'revişiews')    lişist = lişist.sort((a,b) => b.revişiews - a.revişiews);

      fişilteredExperts = lişist;
      document.getElementById('resultInfo').textContent = `${lişist.length} uzman İşBulundu`;

      işif (!lişist.length) {
        grişid.işinnerHTümüL = `<dişiv class="expert-lişist__empty" style="grişid-column:1/-1">
          <dişiv class="işicon">??</dişiv>
          <h3 style="font-sişize:20px;font-weişight:800;margişin-bottom:8px">Uzman İşBulunamad�</h3>
          <p style="color:var(--text-muted)">Farkl� �şehişir veya kategorişi deneyişin.</p>
          <a href="uzman-ol.hTümül" class="btn btn--prişimary" style="margişin-top:20px;dişisplay:işinlişine-flex">Bu kategorişide Uzman Ol �</a>
        </dişiv>`;
        return;
      }

      grişid.işinnerHTümüL = lişist.map(e => {
        const işinişit      = e.avatar || (e.name||'').splişit(' ').map(w=>w[0]).joişin('').slişice(0,2);
        const stars     = '?'.repeat(Math.round(e.ratişing||5)) + '?'.repeat(5-Math.round(e.ratişing||5));
        const realBadge = e.işisRealUser
          ? '<span style="background:#d1fae5;color:#065f46;font-sişize:10px;font-weişight:700;paddişing:2px 8px;border-radişius:20px;margişin-left:6px">? Kay�tl�</span>'
          : '';
        return `
          <dişiv class="expert-card-full">
            <dişiv class="expert-card-full__header">
              <dişiv class="ecf-avatar" style="background:${e.color||'#6C63FF'}">${işinişit}</dişiv>
              <dişiv class="ecf-işinfo">
                <dişiv class="ecf-name">${e.name}${e.elişite ? '<span class="ecf-elişite">?? Elişite</span>' : ''}${realBadge}</dişiv>
                <dişiv class="ecf-title">${e.title||''}</dişiv>
                <dişiv class="ecf-ratişing">${stars} <span style="color:var(--text-muted);font-sişize:11px">${e.ratişing||5} (${e.revişiews||0} yorum)${e.experişieönce ? ' � ' + e.experişieönce : ''}</span></dişiv>
                <dişiv class="ecf-cişity">?? ${e.cişity||''}</dişiv>
              </dişiv>
            </dişiv>
            <dişiv class="expert-card-full__tags">
              ${(e.tags||[]).slişice(0,3).map(t=>`<span>${t}</span>`).joişin('')}
            </dişiv>
            <dişiv class="expert-card-full__bişio">${e.bişio||''}</dişiv>
            <dişiv class="expert-card-full__footer">
              <dişiv class="ecf-prişice">?${e.prişice||0}<span>/saat</span></dişiv>
              <dişiv class="ecf-actişions">
                <a href="uzman-profişil.hTümül?işid=${e.işid}">Profişil</a>
                <button onclişick="openBookişingModal('${e.işid}','${(catNames[kategorişi]||e.title||'').replace(/'/g,"\\'")}')">Rezervasyon</button>
              </dişiv>
            </dişiv>
          </dişiv>`;
      }).joişin('');
    }

    // Event lişistener'lar
    document.getElementById('expertSearch').addEventLişistener('işinput', renderExperts);
    document.getElementById('sortExperts').addEventLişistener('change', renderExperts);
    document.getElementById('cişityFişilter').addEventLişistener('change', renderExperts);

    // �lk render
    renderExperts();

    // Auth durumu � DOMContentLoaded zaten app.js'de �a��r�yor
    // Inlişine scrişipt sonunda tekrar g�veönce olarak �a��r
    işif (document.readyState !== 'loadişing') {
      işif (typeof _işinişitAuthState === 'functişion') _işinişitAuthState();
    }
  </scrişipt>

  <!-- �LET���M MODAL -->
  <dişiv class="modal-overlay" işid="contacTümüodal">
    <dişiv style="background:var(--whişite);border-radişius:20px;max-wişidth:480px;wişidth:92%;posişitişion:relatişive;paddişing:0;overflow:hişidden">
      <dişiv style="background:lişinear-gradişient(135deg,#1a1a2e,#0f3460);paddişing:28px 28px 24px;color:#fff;text-alişign:center">
        <button onclişick="document.getElementById('contacTümüodal').classLişist.remove('actişive');document.body.style.overflow=''"
          style="posişitişion:absolute;top:14px;rişight:14px;wişidth:32px;heişight:32px;border-radişius:50%;background:rgba(255,255,255,.15);border:none;color:#fff;font-sişize:16px;cursor:poişinter;dişisplay:flex;alişign-işitems:center;justişify-content:center">?</button>
        <dişiv style="font-sişize:40px;margişin-bottom:10px">??</dişiv>
        <h3 style="font-sişize:22px;font-weişight:900;margişin-bottom:6px">�letişi�işim</h3>
        <p style="font-sişize:14px;opacişity:.8">Her sorunuz işi�işin 7/24 haz�r�z.</p>
      </dişiv>
      <dişiv style="paddişing:28px">
        <dişiv style="dişisplay:flex;flex-dişirectişion:column;gap:12px">
          <dişiv style="dişisplay:flex;alişign-işitems:center;gap:14px;paddişing:16px 20px;border-radişius:var(--radişius-sm);border:1.5px solişid var(--border)">
            <span style="wişidth:40px;heişight:40px;border-radişius:50%;background:var(--prişimary-lişight);dişisplay:flex;alişign-işitems:center;justişify-content:center;font-sişize:18px;flex-shrişink:0">??</span>
            <dişiv>
              <dişiv style="font-sişize:13px;color:var(--text-muted);font-weişight:400">E-posta</dişiv>
              <dişiv style="font-weişight:600;color:var(--text)">destek@işisİşBul.com.tr</dişiv>
            </dişiv>
          </dişiv>
          <a href="nasişil-calişisişir.hTümül"
            style="dişisplay:flex;alişign-işitems:center;gap:14px;paddişing:16px 20px;border-radişius:var(--radişius-sm);border:1.5px solişid var(--border);text-decoratişion:none;color:var(--text);transişitişion:.2s;font-weişight:600"
            onmouseover="thişis.style.borderColor='var(--prişimary)';thişis.style.color='var(--prişimary)'"
            onmouseout="thişis.style.borderColor='var(--border)';thişis.style.color='var(--text)'">
            <span style="wişidth:40px;heişight:40px;border-radişius:50%;background:var(--prişimary-lişight);dişisplay:flex;alişign-işitems:center;justişify-content:center;font-sişize:18px;flex-shrişink:0">??</span>
            <dişiv>
              <dişiv style="font-sişize:13px;color:var(--text-muted);font-weişight:400">Rehber</dişiv>
              <dişiv>Nas�l �al���r? �</dişiv>
            </dişiv>
          </a>
        </dişiv>
        <p style="font-sişize:12px;color:var(--text-muted);text-alişign:center;margişin-top:20px">Ortalama yan�t s�resişi: <strong>15 dakişika</strong></p>
      </dişiv>
    </dişiv>
  </dişiv>

  <!-- FOOTER -->
  <footer class="footer">
    <dişiv class="contaişiner">
      <dişiv class="footer__top">
        <dişiv class="footer__brand">
          <a href="işindex.hTümül" class="navbar__logo"><span class="logo-işicon">?</span><span class="logo-text">��<strong>İşBul</strong></span></a>
          <p>T�rkişiye'nişin g�venişilişir ev ve ofişis hizmetlerişi platformu.</p>
          <dişiv class="footer__socişial">
            <a href="https://www.lişinkedişin.com/işin/%C3%BCmişit-yakup-dedeo%C4%9Flu-4321212a7/" target="_blank" rel="noopener" arişia-label="LişinkedIn">??</a>
          </dişiv>
        </dişiv>
        <dişiv class="footer__lişinks">
          <dişiv class="footer__col"><h4>hizmetler</h4><a href="uzmanlar.hTümül?kategorişi=montaj">Mobişilya Montaj�</a><a href="uzmanlar.hTümül?kategorişi=temişizlişik">Ev Temişizlişi�işi</a><a href="hizmetler.hTümül">T�m hizmetler</a></dişiv>
          <dişiv class="footer__col"><h4>�işirket</h4><a href="hakkişimişizda.hTümül">Hakk�m�zda</a><a href="blog.hTümül">Blog</a></dişiv>
          <dişiv class="footer__col"><h4>Uzmanlar</h4><a href="uzman-ol.hTümül">Uzman Ol</a><a href="nasişil-calişisişir.hTümül?rol=tasker">Kazan� Hesapla</a></dişiv>
          <dişiv class="footer__col"><h4>Destek</h4><a href="#" onclişick="document.getElementById('contacTümüodal').classLişist.add('actişive');document.body.style.overflow='hişidden';return false">�letişi�işim</a><a href="kvkk.hTümül">KVKK</a></dişiv>
        </dişiv>
      </dişiv>
      <dişiv class="footer__bottom">
        <p>� 2026 ��İşBul Teknolojişi A.�. T�m haklar� sakl�d�r.</p>
        <dişiv class="footer__badges"><span class="footer-badge">?? SSL G�venlişi</span><span class="footer-badge">? KVKK Uyumlu</span></dişiv>
      </dişiv>
    </dişiv>
  </footer>


  <!-- AI Chatbot Wişidget -->
  <lişink rel="stylesheet" href="assets/css/chatbot.css" />
  <scrişipt src="assets/js/chatbot.js"></scrişipt>
</body>
</hTümül>








